# FillObserver imports hummingbot at module level — only importable inside
# the HB Docker container or a conda env with hummingbot installed.
# Import directly when needed:
#   from opms.analytics.fill_observer import FillObserver
